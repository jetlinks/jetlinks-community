package org.jetlinks.community.device.service.data;

import org.jetlinks.community.timeseries.TimeSeriesManager;
import org.jetlinks.core.device.DeviceRegistry;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;

class TimeSeriesDeviceDataStoragePolicyTest {

  @Test
  void doSaveData() {
//      DeviceRegistry registry = Mockito.mock(DeviceRegistry.class);
//      TimeSeriesManager timeSeriesManager = Mockito.mock(TimeSeriesManager.class);
//      DeviceRegistry registry = Mockito.mock(DeviceRegistry.class);
//
//      TimeSeriesRowDeviceDataStoreStoragePolicy timeSeriesDeviceDataStoragePolicy = new TimeSeriesDeviceDataStoragePolicy(registry,timeSeriesManager,new DeviceDataStorageProperties());
  }

  @Test
  void testDoSaveData() {
  }

  @Test
  void doQuery() {
  }

  @Test
  void doQueryPager() {
  }
}